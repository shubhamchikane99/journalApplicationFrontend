import React from 'react';
import './Loader.css'; // Optional: For adding styles

const Loader = () => {
  return (
    <div className="loader">
      <p>Loading...</p>
    </div>
  );
};

export default Loader;
